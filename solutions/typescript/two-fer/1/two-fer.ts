/**
 * This stub is provided to make it straightforward to get started.
 */

export function twoFer(plusOneName: string): string {
  if(!plusOneName){
    return "One for you, one for me.";
  }

  return `One for ${plusOneName}, one for me.`
}
